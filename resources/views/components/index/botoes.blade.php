<div>
    <div class="row mb-2">
        <div class="col-md-12">
            @if($create)
                <a href="{{ route($rota . '.create') }}" class="btn btn-primary">
                    <i class="fa fa-plus"></i> Adicionar
                </a>
            @endif
        </div>
    </div>
</div>
